# 目录结构

## 标准目录结构

```
novels/
└── <项目名称>/
    ├── novel-project.json  # 配置文件（必须）
    ├── progress.json       # 进度文件（必须）
    ├── chapters/           # 章节目录（必须）
    ├── assets/             # 资源目录（必须）
    ├── character_profile.json  # 角色设定（可选）
    └── world_building.json     # 世界观设定（可选）
```